const express=require("express"),morgan=require("morgan"),cors=require("cors"),moment=require("moment"),{Client:Client,Pool:Pool}=require("pg"),{Subject:Subject}=require("rxjs"),pool=new Pool,app=express();app.use(morgan("dev")),app.use(cors()),app.get("/hdb_test",(req,res)=>{res.json("server hdb chat run!")}),app.get("/hdb_msg",(req,res)=>{res.json("hdb all msg")}),app.get("/hdb_user",(req,res)=>{res.json("hdb all msg")}),app.get("/hdb_add/:name/:pw",(req,res)=>{const name=req.params.name,pw=req.params.pw;res.json([name,pw])});const PORT=process.NODE_PORT||6010;app.listen(PORT,()=>console.log(`server started ... @${PORT}`));
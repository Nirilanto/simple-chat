const express=require("express"),morgan=require("morgan"),cors=require("cors"),moment=require("moment"),{Client:Client,Pool:Pool}=require("pg"),{Subject:Subject}=require("rxjs"),pool=new Pool,app=express();app.use(morgan("dev")),app.use(cors()),app.get("/",(req,res)=>{res.json("ok ok ok!!")});const PORT=process.NODE_PORT||6009;app.listen(PORT,()=>console.log(`server started ... @${PORT}`));